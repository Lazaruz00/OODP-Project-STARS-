//This class consists of all methods that can be shared between students and admin

import java.util.ArrayList;
import java.util.List;

/**
 * Interface used to implement methods used by both courses and indexes
 */
public interface SharedMethods {
    void printStds(List<Student> STD);
}
